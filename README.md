# SKCT Result Auto Fetcher 🚀

### Run Setup:
pip install -r requirements.txt

### Create `.env` file:
Rename `.env.example` → `.env`
Fill credentials locally

### Start checker:
python result_checker.py

→ Checks every 15 mins
→ Telegram alert when published
→ Fetches & emails your result automatically
